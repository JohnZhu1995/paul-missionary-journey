# 保罗宣教之旅 - 开发文档

## 📋 项目概述

**游戏名称：** 使徒保罗的宣教之旅  
**主题：** 宣教士的拓荒之路 (The Frontier of Faith)  
**核心机制：** 经文作为解决问题的"武器"，在资源有限与使命迫切之间寻求平衡

---

## 🎯 设计哲学

### 游戏目的（三层维度）

**A. 认知维度：地理与历史的连贯性**
- 通过地图建立空间感
- 城市民俗百科增强历史沉浸
- 保罗旅程的地理可视化

**B. 情感维度：共情与代入感**
- 信念值(Faith)作为生存机制
- 被赶出城、遭遇海难时信念下降
- 通过灵修笔记回顾经文恢复信念

**C. 实践维度：经文的"武器化"与"内化"**
- 经文是解决挑战的工具
- 收藏经文从收集任务变为生存必需
- 选择合适的经文作为论据

### 核心冲突
**"资源的有限"与"使命的迫切"**

玩家需要平衡：
- 信念(Faith)：生命值，归零进入禁食祷告
- 供给(Supplies)：旅行消耗
- 影响力(Influence)：解锁选项、应对事件

---

## 🏗️ 系统架构

### 1. 资源管理系统 (Resource System)

```javascript
GameState.resources = {
    faith: 100,        // 信念（生命值）
    supplies: 80,      // 供给（旅行消耗）
    influence: 20      // 影响力（解锁选项）
};
```

**设计意图：**
- Faith作为"生命值"，体现精神层面的脆弱性
- Supplies体现物质层面的限制
- Influence代表属灵权柄和社会认可

### 2. 经文装备系统 (Scripture Equipment System)

**Phase 1 重构核心：** 将静态经文变为属灵装备

#### 经文类型（12种）

| 类型 | 图标 | 用途 | 示例经文 |
|------|------|------|---------|
| 呼召 | 📜 | 证明神圣差派 | 徒13:1-2 |
| 预备 | 🙏 | 战前属灵预备 | 徒13:3 |
| 权柄 | ⚔️ | 斥责抵挡者 | 徒13:9-10 |
| 审判 | ⚡ | 神圣审判宣告 | 徒13:11 |
| 讲道 | 📢 | 开启听道之门 | 徒13:16 |
| 福音 | ✝️ | 救恩核心信息 | 徒13:38-39 |
| 观察 | 👁️ | 辨别信心 | 徒14:8 |
| 神迹 | ✨ | 医治彰显大能 | 徒14:9-10 |
| 警戒 | ⚠️ | 防止偶像崇拜 | 徒14:11 |
| 纠正 | 🔄 | 拒绝不当敬拜 | 徒14:15 |
| 逼迫 | 🪨 | 忍受苦难 | 徒14:19 |
| 复活 | 🌅 | 重新得力 | 徒14:20 |
| 劝勉 | 💪 | 坚固教会 | 徒14:22 |

#### 数据结构

```javascript
GameData.verses = {
    acts13_1: {
        text: "...",
        reference: "使徒行传 13:1-2",
        type: "calling",           // 装备类型
        power: "divine_mandate",   // 能力标识
        description: "...",        // 功能描述
        usage: "...",             // 使用场景说明
        strength: 25,             // 说服力伤害值
        consume: false,           // 是否消耗
        maxUses: null,            // 最大使用次数
        usedIn: ["scene_tag"],    // 适用场景标签
        effect: {                 // 使用效果
            faith: 15,
            influence: 10,
            enemy_resistance: -30,
            gospel_saturation: 20  // 增加福音饱和度
        }
    }
};
```

### 3. 经文对决系统 (Scripture Debate System)

**Phase 1 核心重构：** 替换技能战斗为经文选择

#### 对决流程

```
1. 触发对决 → 显示敌人信息和说服力进度条
2. 显示可用经文（已收藏且适用当前场景）
3. 玩家选择经文作为论据
4. 计算伤害：
   - 基础伤害 = 经文strength
   - 弱点加成 = 经文type匹配enemy.weakness ? 1.5倍 : 1倍
5. 应用经文效果（资源变化）
6. 敌人反击（损失信念）
7. 检查胜利/失败条件
```

#### 代码实现 (game.js)

```javascript
// 初始化对决
function initScriptureDebate(enemyId) {
    const enemy = scaleEnemyByDifficulty(enemyId);
    GameState.battleState = {
        enemy: enemy,
        enemyCurrentResistance: enemy.resistance,
        turn: 1,
        usedVerses: []
    };
    renderScriptureDebateScreen();
}

// 执行对决回合
function executeScriptureDebateTurn(verseKey) {
    const verse = useVerse(verseKey);
    let damage = verse.strength;
    
    // 弱点判定
    if (enemy.weakness.includes(verse.type)) {
        damage *= 1.5;  // 1.5倍伤害
    }
    
    // 应用效果
    applyVerseEffects(verse.effect);
    
    // 敌人反击
    enemyCounterAttack();
}
```

### 4. 旅行事件系统 (Travel Event System)

#### 事件类型

| 事件 | 基础概率 | 难度 | 效果 |
|------|---------|------|------|
| 顺风顺水 | 35% | 1 | 轻微消耗 |
| 小风浪 | 25% | 2 | 中等消耗 |
| 海上风暴 | 20% | 3 | 需Faith应对 |
| 路遇强盗 | 15% | 4 | 需Influence化解 |
| 船只失事 | 5% | 5 | 严重消耗 |

#### 动态概率算法

```javascript
// 技能越高，负面事件概率越低
const skillFactor = Math.min(0.2, skillPoints * 0.02);

// 供给充足时，风险事件概率降低
const supplyFactor = resources.supplies > 50 ? -0.05 : 
                    resources.supplies < 20 ? 0.05 : 0;

// 最终概率
if (event.difficulty <= 2) {
    return baseProb + skillFactor * 0.5;  // 正面事件增加
} else {
    return Math.max(0.02, baseProb - skillFactor + supplyFactor);  // 负面事件减少
}
```

### 5. 城市系统 (City System)

#### 城市数据结构

```javascript
GameData.cities = {
    lystra: {
        name: "路司得",
        description: "...",
        gospel_saturation: 0,  // 福音饱和度 0-100
        traits: {
            people_temperament: "fickle",    // 民性
            opposition_level: 5,             // 敌对等级 1-5
            religious_background: "pagan"    // 宗教背景
        },
        lore: {
            location: "地理位置",
            customs: "风俗习惯",
            historical_note: "历史背景",
            challenge: "宣教挑战"
        }
    }
};
```

---

## 📊 动态难度算法

### 公式

```
D = S + σ

D = 目标难度
S = 技能等级总和 (辩论+神迹+忍耐)
σ = 扰动项 = 0.5 + 城市进度×0.1 ± 随机波动
```

### 应用

```javascript
// 计算目标难度
function calculateTargetDifficulty(skills, cityIndex) {
    const S = Object.values(skills).reduce((sum, s) => sum + s.level, 0);
    const sigma = 0.5 + (cityIndex * 0.1) + (Math.random() * 0.5 - 0.25);
    return Math.max(1, S + sigma);
}

// 根据目标难度缩放敌人
function scaleEnemy(enemy, targetDifficulty) {
    const scaleFactor = targetDifficulty / enemy.difficulty;
    return {
        ...enemy,
        resistance: Math.floor(enemy.baseResistance * scaleFactor)
    };
}
```

---

## 🔄 重构历程

### Phase 1: 经文武器化 (已完成)

**时间：** 2026-02-12  
**提交：** 014454e

**改动内容：**
1. 重构13段经文为12种属灵装备类型
2. 实现经文对决系统（替换技能战斗）
3. 添加城市福音饱和度基础字段
4. 为7个城市添加民俗百科
5. 优化UI：卡片式经文选择界面

**关键决策：**
- 保留技能系统但不用于战斗（为Phase 3评价系统预留）
- 经文消耗机制：核心经文不消耗，强力经文限制次数
- 弱点系统：经文类型匹配敌人弱点造成1.5倍伤害

---

## 📝 开发规范

### 代码风格
- 使用原生JavaScript，无框架依赖
- 函数命名：动词开头，驼峰式（如 `initScriptureDebate`）
- 常量命名：大写下划线（如 `SAVE_KEY`）
- 注释：关键算法和复杂逻辑必须注释

### 文件结构
```
paul-missionary-journey/
├── index.html          # 主页面
├── game.js            # 游戏核心逻辑
├── data.js            # 游戏数据和状态管理
├── styles.css         # 样式
├── manifest.json      # PWA配置
├── service-worker.js  # 离线缓存
└── docs/              # 开发文档
    ├── README.md      # 项目说明
    ├── DESIGN.md      # 设计文档（本文件）
    └── CHANGELOG.md   # 更新日志
```

### Git规范
- 提交信息使用中文，清晰描述改动
- 每个Phase完成后提交并推送
- 提交信息格式：
  ```
  Phase X: 简短描述
  
  详细说明...
  - 改动1
  - 改动2
  ```

---

## 🎯 Phase 2 & 3 规划

### Phase 2: 福音饱和度系统

**目标：** 让城市从静态关卡变为动态生态

**实现内容：**
1. 经文对决胜利增加城市福音饱和度
2. 饱和度影响事件概率（饱和度高→负面事件减少）
3. 饱和度影响战斗难度（饱和度高→敌人抵抗力降低）
4. 点击城市显示百科卡片UI

**预计时间：** 2小时

### Phase 3: 教会稳固度评价

**目标：** 多维度通关评价，增强重玩价值

**实现内容：**
1. 4维度评价：
   - 根基：测验得分
   - 门徒：转化城市数
   - 长老：收集经文数
   - 忍耐：经历逼迫后的剩余信念
2. 4种结局等级：使徒级/宣教士级/门徒级/逃遁级
3. 评价可视化界面

**预计时间：** 1-2小时

---

## 📚 技术参考

### LocalStorage 键名
- `paulJourney_saveData_chapter1_v2`: 当前版本存档
- `paulJourney_saveData_chapter1`: 旧版本存档（兼容）

### 存档数据结构
```javascript
{
    currentScene: "antioch_start",
    completedCities: [],
    collectedVerses: [],
    totalScore: 0,
    resources: { faith: 100, supplies: 80, influence: 20 },
    skills: { debate: {...}, miracle: {...}, endurance: {...} },
    currentCityIndex: 0,
    verseUsage: {},  // 经文使用次数追踪
    timestamp: "...",
    version: "2.1"
}
```

---

## 🤝 开发者须知

### 添加新经文
1. 在 `data.js` 的 `GameData.verses` 中添加
2. 指定 `type`, `strength`, `effect`, `usedIn`
3. 决定 `consume` 和 `maxUses`
4. 确保在对应场景可收集

### 添加新敌人
1. 在 `data.js` 的 `GameData.spiritualBattles` 中添加
2. 指定 `weakness`（对应的经文类型）
3. 设置 `baseResistance` 和 `difficulty`
4. 在场景数据中使用 `battleEnemy` 引用

### 添加新城市
1. 在 `GameData.cities` 中添加
2. 填写 `traits` 和 `lore`
3. 设置初始 `gospel_saturation: 0`
4. 在场景数据中关联

---

*最后更新：2026-02-12*  
*版本：2.1*  
*作者：开发团队*
