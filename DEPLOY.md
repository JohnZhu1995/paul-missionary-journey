# 使徒保罗的宣教之旅 - PWA 部署指南

## 📱 PWA功能介绍

本游戏已打包为渐进式Web应用（PWA），具备以下功能：

- ✅ **离线游玩** - 无需网络连接也能玩
- ✅ **添加到主屏幕** - 像原生应用一样从桌面启动
- ✅ **自动缓存** - 首次加载后自动缓存所有资源
- ✅ **更新提醒** - 有新版本时自动更新
- ✅ **全屏体验** - 无浏览器地址栏，沉浸式游戏

---

## 🚀 部署方法

### 方法一：静态网站托管（推荐）

#### 1. GitHub Pages
```bash
# 1. 创建GitHub仓库
# 2. 上传所有文件到仓库
# 3. 在Settings > Pages中启用GitHub Pages
# 4. 选择main分支作为源
```

#### 2. Netlify
```bash
# 1. 访问 https://www.netlify.com/
# 2. 将项目文件夹拖拽到Netlify
# 3. 获得免费域名（如 paul-journey-123456.netlify.app）
# 4. 可绑定自定义域名
```

#### 3. Vercel
```bash
# 1. 访问 https://vercel.com/
# 2. 导入GitHub仓库或直接上传
# 3. 自动部署，获得域名
```

#### 4. 腾讯云/阿里云对象存储
```bash
# 1. 创建对象存储Bucket
# 2. 设置为静态网站托管
# 3. 上传所有文件
# 4. 配置自定义域名（可选）
```

### 方法二：本地服务器测试

#### 使用Python（临时服务器）
```bash
# Python 3
cd 保罗宣教之旅
python -m http.server 8000

# 访问 http://localhost:8000
```

#### 使用Node.js
```bash
# 安装http-server
npm install -g http-server

# 启动服务器
cd 保罗宣教之旅
http-server -p 8080

# 访问 http://localhost:8080
```

#### 使用VS Code Live Server插件
1. 在VS Code中安装"Live Server"插件
2. 右键点击index.html
3. 选择"Open with Live Server"

---

## 📲 安装到手机桌面

### Android (Chrome)
1. 用Chrome浏览器打开游戏网址
2. 点击底部弹出的"添加到主屏幕"提示
3. 或点击菜单（⋮）→ "添加到主屏幕"
4. 确认添加
5. 桌面会出现应用图标

### iOS (Safari)
1. 用Safari浏览器打开游戏网址
2. 点击底部"分享"按钮（⬆️）
3. 选择"添加到主屏幕"
4. 可自定义名称，点击"添加"
5. 主屏幕会出现应用图标

### iOS (Chrome/Edge)
1. 用Chrome或Edge打开游戏
2. 点击菜单（⋮）
3. 选择"添加到主屏幕"或"安装应用"
4. 确认添加

### 电脑 (Chrome/Edge)
1. 打开游戏网址
2. 地址栏右侧会出现"安装"图标（📥）
3. 点击安装
4. 或使用菜单 → "更多工具" → "创建快捷方式"

---

## 🔧 重要配置

### 必须开启HTTPS
PWA要求网站必须使用HTTPS协议（本地开发除外）。

#### 检查清单：
- [ ] 网站已部署到HTTPS服务器
- [ ] manifest.json路径正确
- [ ] service-worker.js路径正确
- [ ] 所有资源（CSS、JS、图片）使用相对路径或HTTPS

### 推荐Web服务器配置

#### Nginx
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;
    
    root /var/www/paul-journey;
    index index.html;
    
    # 启用gzip压缩
    gzip on;
    gzip_types text/plain text/css application/javascript application/json;
    
    # 缓存静态资源
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Service Worker不缓存
    location = /service-worker.js {
        add_header Cache-Control "no-cache";
        expires off;
    }
}
```

#### Apache
```apache
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
</IfModule>

<FilesMatch "^service-worker\.js$">
    <IfModule mod_headers.c>
        Header set Cache-Control "no-cache"
    </IfModule>
</FilesMatch>
```

---

## 🎨 自定义配置

### 修改应用信息

编辑 `manifest.json`：
```json
{
  "name": "你的应用名称",
  "short_name": "短名称",
  "description": "应用描述",
  "theme_color": "#主题色",
  "background_color": "#背景色"
}
```

### 更换图标

1. 准备正方形PNG图标（512x512像素）
2. 使用在线工具生成多尺寸图标：
   - https://www.pwabuilder.com/imageGenerator
   - https://app-manifest.firebaseapp.com/
3. 将生成的图标放入 `icons/` 文件夹
4. 更新 `manifest.json` 中的图标路径

### 修改主题色

编辑 `index.html` 中的 meta 标签：
```html
<meta name="theme-color" content="#你的颜色">
```

编辑 `manifest.json`：
```json
{
  "theme_color": "#你的颜色",
  "background_color": "#你的颜色"
}
```

---

## 📊 性能优化

### 首次加载优化
- 所有资源已压缩（约75KB总大小）
- 支持Gzip/Brotli压缩（可减少60%体积）
- 图片使用SVG/Canvas动态生成（减少HTTP请求）

### 缓存策略
- **静态资源**：缓存1年（CSS、JS、图标）
- **HTML页面**：不缓存（确保获取最新版本）
- **Service Worker**：每次访问检查更新

### 更新机制
1. 修改代码后更新 `service-worker.js` 中的版本号
2. 用户下次访问时会自动下载新版本
3. 刷新页面后使用新版本

---

## 🐛 常见问题

### Q1: 为什么无法安装PWA？
**A:** 请检查：
- 网站是否使用HTTPS
- manifest.json 是否可访问
- Service Worker 是否成功注册（查看控制台）

### Q2: 离线后无法玩？
**A:** 
- 首次必须在联网状态下加载完整游戏
- 检查浏览器缓存是否被清除
- 查看控制台是否有Service Worker错误

### Q3: iOS上"添加到主屏幕"后打开是空白？
**A:** 
- 确保使用Safari添加
- 检查是否开启了"私密浏览"模式
- 尝试清除Safari缓存后重新添加

### Q4: 如何清除缓存重新加载？
**A:** 
- Chrome: F12 → Application → Service Workers → Unregister
- 或访问 chrome://serviceworker-internals/
- 移动端：卸载后重新安装

### Q5: 更新后还是旧版本？
**A:** 
- 强制刷新：Ctrl+Shift+R (Windows) 或 Cmd+Shift+R (Mac)
- 或关闭所有标签页重新打开
- 等待Service Worker激活（最多24小时）

---

## 📈 进阶功能

### 推送通知（可选）
如需添加推送通知功能：
1. 注册Firebase Cloud Messaging
2. 获取服务器密钥
3. 在后端实现推送服务
4. 修改 `service-worker.js` 中的推送处理逻辑

### 后台同步（可选）
如需在网络恢复时自动同步数据：
1. 修改 `service-worker.js` 中的 sync 事件处理
2. 在 `game.js` 中使用 Background Sync API

### 分享功能（可选）
添加原生分享按钮：
```javascript
if (navigator.share) {
  navigator.share({
    title: '保罗的宣教之旅',
    text: '来一起玩这个圣经学习游戏！',
    url: window.location.href
  });
}
```

---

## 📞 技术支持

如遇问题，请检查：
1. 浏览器控制台错误信息
2. Application > Service Workers 状态
3. Network 面板查看资源加载情况
4. Lighthouse PWA审核报告（Chrome DevTools）

---

## ✅ 部署检查清单

- [ ] 文件上传到服务器
- [ ] 网站可通过HTTPS访问
- [ ] manifest.json 可正常访问
- [ ] service-worker.js 可正常访问
- [ ] 手机浏览器可以正常打开
- [ ] 可以成功"添加到主屏幕"
- [ ] 离线状态下可以正常游戏
- [ ] 图标显示正常
- [ ] 主题色正确显示

---

## 🎉 完成部署

恭喜你！现在用户可以通过以下方式玩游戏：
1. 在浏览器中访问你的网址
2. 添加到手机/电脑桌面
3. 离线时也能随时打开玩

愿神祝福这个游戏，帮助更多人了解保罗的宣教旅程！
